language: cpp
compiler: gcc
sudo: required
matrix:
  include:
    - os: osx
      osx_image: xcode10.1
    - os: linux
    - os: windows
      script:
       - cd Platform.Data.Triplets.Kernel
       - mingw32-make
       - dir
       - test
script:
 - cd Platform.Data.Triplets.Kernel
 - make
 - export LD_LIBRARY_PATH=$(pwd)
 - ./test
